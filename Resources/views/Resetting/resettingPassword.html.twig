{% extends "@PdUser/base.html.twig" %}

{% block head_title %}{{ 'security.resetpw_title'|trans }}{% endblock %}

{% block body %}
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    <div class="container">
        <div class="auth resetting">
            <!-- Resetting Form -->
            {{ form_start(form) }}
                <div class="header">
                    <h2 class="title">{{ 'security.resetpw_title'|trans }}</h2>
                    <div class="float-icon">
                        <a href="{{ path('security_login') }}" class="add" title="{{ 'security.login_title'|trans }}"><i class="material-icons">security</i></a>
                    </div>
                </div>

                {{ form_widget(form) }}

                <div class="row mt-4">
                    <div class="col-sm-12 col-xs-12 text-center">
                        <button type="submit" id="reset_password_submit" class="btn btn-primary btn-lg btn">{{ 'security.resetpw_title'|trans }}</button>
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
